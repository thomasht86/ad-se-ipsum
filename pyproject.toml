[project]
name = "arete-blog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-material[imaging]",
    "jupyter",
    "openai",
    "python-dotenv>=1.0.1",
    "mkdocs-marimo",
]
